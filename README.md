# Rabit Tasks

The internal task board for the Rabit founding team — one backlog, one board,
a shared timeline. Static site, no build step; state lives in the browser or,
when connected, in a shared Supabase workspace (Settings → Shared workspace).

This is the deploy repository for GitHub Pages. Development history lives in
the private `Main-Dashboard` repository.
